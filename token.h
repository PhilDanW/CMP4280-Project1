#ifndef TOKEN_H
#define TOKEN_H

#define MAX 9
#define LIMIT 200

typedef enum {
	IDENTIFIER,
	KEYWORD,
	NUMBER,	
	EQUAL,
	GREATER,
	LESS,
	COLON,
	PLUS,
	MINUS,
	ASTERIK,
	SLASH,
	PERCENT,
	DOT,
	L_PARENT,
	R_PARENT,
	COMMA,
	L_BRACE,
	R_BRACE,
	SEMICOLON,
	L_BRACKET,
	R_BRACKET,
	
	//keywords
	BEGIN_KEYWORD,
	END_KEYWORD,
	LOOP_KEYWORD,
	WHILE_KEYWORD,
	VOID_KEYWORD,
	EXIT_KEYWORD,
	GETTER_KEYWORD,
	OUTTER_KEYWORD,
	MAIN_KEYWORD,
	IF_KEYWORD,
	THEN_KEYWORD,
	ASSIGN_KEYWORD,
	DATA_KEYWORD,
	PROC_KEYWORD,
	EOT // end of token
} TokenType;

typedef struct {
	TokenType tokenType;
	char instance[100];
	int lineNum;
} Token;

Token* tokens;

#endif
